#cdnp
    .topicbox.topicbox-1.mui--z4 
        .topicbox--middle 
            | Only user-specific data is truly dynamic; which browser he uses is not.
            br 
            | Maximize static content and use a CDN to cache it at the edge for speed. 
    .thumb.thumb-1.thumb-1--selected.mui--z1
    .thumb.thumb-2.mui--z1
    .thumb.thumb-3.mui--z1
    .thumb-txt.thumb-txt-1 #{m1}
    //-#tbd.headline.headline__sub(style='top:27.3vw') [TBD Next Steps] 
    img#cdnimg(src='../100-basics/res/cdn.png')

script.
    var animateMaterial = true;

    function cdnLoaded(){ 
        /*if (animateMaterial)
        {
            var tl = new TimelineMax();
            tl[4] = tl;
            tl.to($('#materialp #tbd'), 3, {opacity:100, ease:Power1.easeIn}, '+=0');
            tl.play();
        }*/
    } 

    function page4Setup(animate){
        animatePage4 = animate;
        if (animatePage4)
            $('#cdnp #tbd').css('opacity',0);
    }

